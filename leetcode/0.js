/**
 * [0. 模板](https://leetcode.cn/xxx)
 * @param {number} num
 * @return {number}
 */
var fun = function (num) {
    return num;
};

const { expect } = require('chai');
it('0', () => {
    expect(fun(1)).to.eq(1);
});
